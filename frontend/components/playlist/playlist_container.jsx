import { connect } from 'react-redux'
import React from 'react';
import PlaylistItem from './playlist_item';

const mSTP = state => {
  return {

  };
};

const mDTP = dispatch => {
  return {

  };
};

export default connect(mSTP, mDTP)(PlaylistItem);